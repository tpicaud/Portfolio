---
import type { IHeroProps }  from "@types";

const { name, about } = Astro.props as IHeroProps;
---

<section>
  <p class="dark:text-light mt-[100px]">Hey, I’m</p>
  <h1>{name}</h1>
  <p class="dark:text-light text-justify text-base md:text-lg mb-16">
    {about}
  </p>
</section>
